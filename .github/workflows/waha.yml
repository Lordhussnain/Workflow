name: Download Waha ARM Image and Save as TAR

on:
  workflow_dispatch:

jobs:
  download-and-save:
    runs-on: ubuntu-latest

    steps:
      - name: 🐳 Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: 🧰 Create output directory
        run: mkdir -p output

      - name: 📥 Pull devlikeapro/waha:arm-2025.10.3
        run: |
          IMAGE_NAME="devlikeapro/waha:arm-2025.10.3"
          echo "Pulling image: ${IMAGE_NAME}"
          docker pull ${IMAGE_NAME}

      - name: 🗜 Save image to TAR
        run: |
          IMAGE_NAME="devlikeapro/waha:arm-2025.10.3"
          OUTPUT_PATH="output/waha-arm-2025.10.3.tar"
          echo "Saving ${IMAGE_NAME} to ${OUTPUT_PATH}"
          docker save ${IMAGE_NAME} -o "${OUTPUT_PATH}"

      - name: 📤 Upload TAR file as artifact
        uses: actions/upload-artifact@v4
        with:
          name: waha-arm-tar
          path: output/waha-arm-2025.10.3.tar
